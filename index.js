const testVar = {}

function testFunc() {
  return "hi"
}


function superbowlWin(record){
  return record.find(rec =>{
    if(rec.result === "W"){
      let data = rec.year
      return data
        } else {
        return undefined
      }}
)}
